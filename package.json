{
  "name": "vehicle-rental-platform",
  "version": "1.0.0",
  "description": "Complete Vehicle Rental Web Application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["vehicle", "rental", "react", "nodejs", "mysql"],
  "author": "Vehicle Rental Platform",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
